<UserControl x:Class="Kakuro.Windows.MVVM.View.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Kakuro.Windows.MVVM.View" 
             xmlns:viewModel="clr-namespace:Kakuro.Windows.MVVM.ViewModel" d:DataContext="{d:DesignInstance Type=viewModel:LoginViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.DataContext>
        <viewModel:LoginViewModel/>
    </UserControl.DataContext>
    
    <StackPanel>
        
        <TextBox Tag="Ім'я користувача" 
                 Text="{Binding UserName, Mode=TwoWay}"
                 Style="{StaticResource TextBoxTheme}"
                 Margin="0,0,0,10"/>

        <PasswordBox Tag="Пароль"
                 PasswordChanged="PasswordBox_PasswordChanged"
                 Style="{StaticResource PasswordBoxTheme}"
                 Margin="0,0,0,10"/>
        
        <Button Content="Увійти" 
                Style="{StaticResource ButtonTheme}"
                Command="{Binding LoginCommand}"/>
    </StackPanel>
</UserControl>
